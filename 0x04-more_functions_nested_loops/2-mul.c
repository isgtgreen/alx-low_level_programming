#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with the first number
 * Return: The result of the multiplication
 * beween x parameters and y parameters
 */

int mul(int x, int y)
{
	return (x * y);
}
